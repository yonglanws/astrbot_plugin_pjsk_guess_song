# 更新日志

### v1.2.0

- **题库 master 数据自动同步**：歌曲题库改从 Haruki master 数据（`musics.json` + `musicVocals.json`）自动拉取，每 24 小时检查一次，新歌随游戏版本更新自动入库；中文歌名来自 Moesekai 同款翻译源；GitHub API 不可用时回退 jsDelivr，dataVersion 未变化时跳过大文件下载
- **新增日服/国服题库切换**：`猜歌切换日服题库` / `猜歌切换国服题库`，按群独立记忆，结算显示当前题库服务器
- **新增 QQ 官方机器人 markdown 机制**：官机开局/结算消息以 markdown 渲染，开局附「仅退出本局 / 退出自动模式」连接按钮；结算附切换题库、绑定QQ、个人分数、排行榜连接与快捷入口
- **排行榜改版**：本群/本地排行榜改为与猜卡面插件一致的横向表格样式（奖牌、未绑定QQ徽章、正确率等）
- **统一退出机制**：`仅退出本局` 只结束当前对局（自动模式继续），`退出自动模式` 停止自动续局（本局继续打完），任何时候可触发；旧的 `退出` 指令废除
- **自动模式优化**：自动模式期间不出现任何 markdown 按钮，结算只显示结果、歌名与下一局提示，退出自动模式后恢复
- 新增 `default_server`、`update_interval_hours`、`connect_link_template` 配置项
- 猜歌分数新增 `猜歌个人分数` 别名，补录 `自动猜歌` 指令说明
